version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: test
          command: echo $digital_ip && apt update && apt install expect
      - run:
          name: deploy
          command: bash .circleci/deploy-ssh.sh